{
  "buildCommand": "cd ../.. && pnpm turbo run build --filter=@near/near-org",
  "outputDirectory": "apps/near-org/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/publish-scheduled",
      "schedule": "* * * * *"
    }
  ]
}
